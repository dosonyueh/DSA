#include <stdio.h>
#include <stdlib.h>

struct node{
	int key;
	node *left;
	node *right;
};
typedef struct node node_t;


int main()
{
	int num_of_node,i,key,left,right;
	scanf("%d",&num_of_node);
	for(i = 0 ; i < num_of_node ; i++)
	{
		scanf("%d %d %d",&key,&left,&right);
		while((c = getchar()) != EOF && c != '\n');
				
	}
}
